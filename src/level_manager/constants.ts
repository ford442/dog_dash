export const DEFAULT_FOG_FAR = 80;
export const DEFAULT_FOG_NEAR = 20;
export const FOG_FAR_DENSITY_FACTOR = 5;
export const FOG_NEAR_DENSITY_FACTOR = 3;

export const STREAM_AHEAD_START = 80;
export const STREAM_AHEAD_END = 550;
/** Wait until this much unpopulated X is queued before spawning (avoids full density dumps into 1-frame slices). */
export const MIN_FOLIAGE_STREAM_WIDTH = 40;
/** FoliageDensity numbers are counts per this many world units. */
export const FOLIAGE_DENSITY_UNIT = 100;
