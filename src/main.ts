/**
 * Dog Dash / Space Dash — thin entry point.
 * All game logic lives under ./main/ and shared modules (game_systems, game_runtime, etc.).
 */
import { bootstrap } from './main/bootstrap';

bootstrap();
